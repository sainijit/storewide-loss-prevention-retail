# Auto-generated by poi_stream_density.py — do not edit
# Stream density: 3 scenes

services:
  lp-cams-3:
    image: linuxserver/ffmpeg:version-8.0-cli
    command: "-nostdin -re -stream_loop -1 -i /workspace/media/Camera_01.mp4 -c:v copy -an -f rtsp -rtsp_transport tcp rtsp://mediaserver:8554/Camera_01-3"
    volumes:
      - vol-sample-data:/workspace/media
    networks:
      - storewide-lp
    depends_on:
      - mediaserver
    restart: "no"

  lp-video-3:
    image: docker.io/intel/dlstreamer-pipeline-server:${DLSTREAMER_VERSION:-2026.1.0-20260331-weekly-ubuntu24}
    networks:
      storewide-lp:
    tty: true
    entrypoint: ["./run.sh"]
    devices:
      - "/dev/dri:/dev/dri"
    group_add:
      - "109"
      - "110"
      - "992"
    device_cgroup_rules:
      - "c 189:* rmw"
      - "c 209:* rmw"
      - "a 189:* rwm"
    depends_on:
      broker:
        condition: service_started
      ntpserv:
        condition: service_started
      lp-cams-3:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-I", "-s", "http://localhost:8080/pipelines"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    environment:
      - RUN_MODE=EVA
      - GENICAM=Balluff
      - GST_DEBUG=1,gencamsrc:2
      - ADD_UTCTIME_TO_METADATA=true
      - APPEND_PIPELINE_NAME_TO_PUBLISHER_TOPIC=false
      - MQTT_HOST=broker.scenescape.intel.com
      - MQTT_PORT=1883
      - REST_SERVER_PORT=8080
      - HTTPS_PROXY=${HTTPS_PROXY}
      - https_proxy=${https_proxy}
      - HTTP_PROXY=${HTTP_PROXY}
      - http_proxy=${http_proxy}
      - NO_PROXY=mediaserver,${NO_PROXY}
      - no_proxy=mediaserver,${no_proxy}
    configs:
      - source: lp-config-3
        target: /home/pipeline-server/config.json
    volumes:
      - ../scenescape/dlstreamer-pipeline-server/user_scripts:/home/pipeline-server/user_scripts
      - vol-dlstreamer-pipeline-root-3:/var/cache/pipeline_root:uid=1999,gid=1999
      - vol-sample-data:/home/pipeline-server/videos
      - vol-models:/home/pipeline-server/models
    secrets:
      - source: root-cert
        target: certs/scenescape-ca.pem
    restart: always
    pids_limit: 1000

  lp-cams-4:
    image: linuxserver/ffmpeg:version-8.0-cli
    command: "-nostdin -re -stream_loop -1 -i /workspace/media/Camera_01.mp4 -c:v copy -an -f rtsp -rtsp_transport tcp rtsp://mediaserver:8554/Camera_01-4"
    volumes:
      - vol-sample-data:/workspace/media
    networks:
      - storewide-lp
    depends_on:
      - mediaserver
    restart: "no"

  lp-video-4:
    image: docker.io/intel/dlstreamer-pipeline-server:${DLSTREAMER_VERSION:-2026.1.0-20260331-weekly-ubuntu24}
    networks:
      storewide-lp:
    tty: true
    entrypoint: ["./run.sh"]
    devices:
      - "/dev/dri:/dev/dri"
    group_add:
      - "109"
      - "110"
      - "992"
    device_cgroup_rules:
      - "c 189:* rmw"
      - "c 209:* rmw"
      - "a 189:* rwm"
    depends_on:
      broker:
        condition: service_started
      ntpserv:
        condition: service_started
      lp-cams-4:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-I", "-s", "http://localhost:8080/pipelines"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    environment:
      - RUN_MODE=EVA
      - GENICAM=Balluff
      - GST_DEBUG=1,gencamsrc:2
      - ADD_UTCTIME_TO_METADATA=true
      - APPEND_PIPELINE_NAME_TO_PUBLISHER_TOPIC=false
      - MQTT_HOST=broker.scenescape.intel.com
      - MQTT_PORT=1883
      - REST_SERVER_PORT=8080
      - HTTPS_PROXY=${HTTPS_PROXY}
      - https_proxy=${https_proxy}
      - HTTP_PROXY=${HTTP_PROXY}
      - http_proxy=${http_proxy}
      - NO_PROXY=mediaserver,${NO_PROXY}
      - no_proxy=mediaserver,${no_proxy}
    configs:
      - source: lp-config-4
        target: /home/pipeline-server/config.json
    volumes:
      - ../scenescape/dlstreamer-pipeline-server/user_scripts:/home/pipeline-server/user_scripts
      - vol-dlstreamer-pipeline-root-4:/var/cache/pipeline_root:uid=1999,gid=1999
      - vol-sample-data:/home/pipeline-server/videos
      - vol-models:/home/pipeline-server/models
    secrets:
      - source: root-cert
        target: certs/scenescape-ca.pem
    restart: always
    pids_limit: 1000

  poi-backend:
    environment:
      RTSP_PREWARM_CAMERAS: "Camera_01,Camera_02,Camera_01-3,Camera_01-4"
      MQTT_IMAGE_CAMERAS: "Camera_01,Camera_02,Camera_01-3,Camera_01-4"
      STREAM_DENSITY: "3"

configs:
  lp-config-3:
    file: ${PIPELINE_CONFIG_3:-/home/intel/sainijit/storewide-loss-prevention-retail/scenescape/dlstreamer-pipeline-server/person-of-interest-Camera_01-3-pipeline-config.json}
  lp-config-4:
    file: ${PIPELINE_CONFIG_4:-/home/intel/sainijit/storewide-loss-prevention-retail/scenescape/dlstreamer-pipeline-server/person-of-interest-Camera_01-4-pipeline-config.json}

volumes:
  vol-dlstreamer-pipeline-root-3:
  vol-dlstreamer-pipeline-root-4:
